// Colors
// ----------------------------------------------------------------------------
$orange = #ff5027
$black = #000
$white = #fff

$medium = 'screen and (min-width: 667px)'
$large = 'screen and (min-width: 1025px)'

Hover()
  .Mouse &:hover,
  &:active
    {block}
    
Active = Tap = Hover

vw(num, $totalWidth = 1366)
  $res = num/$totalWidth * 100
  $res = unit($res, 'vw')
  
  round($res, 4)
  
